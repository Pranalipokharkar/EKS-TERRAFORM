# EKS-Terraform
Setup VM
Install AWS CLI and configure it.
Insatall eksctl
Install Kubectl
Install Terraform
Terraform init
Terraform apply
aws eks update-kubeconfig --region ap-south-1 --name eks-cluster
